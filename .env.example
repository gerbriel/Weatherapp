# Environment Variables Template
# Copy this file to .env and fill in your actual values for LOCAL DEVELOPMENT ONLY
# ⚠️  WARNING: NEVER commit real API keys to git! ⚠️

# 🔒 SECURITY: For production deployment, set these as GitHub Repository Secrets:
# Repository Settings > Secrets and variables > Actions > New repository secret
#
# Required GitHub Repository Secrets:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY  
# - SUPABASE_SERVICE_ROLE_KEY (for database migrations)
# - RESEND_API_KEY (for email functionality)

# Local Development Configuration (use placeholder values for commit)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Email Configuration (for development only)
RESEND_API_KEY=your_resend_api_key_here
FROM_EMAIL=weather@resend.dev

# CMIS API Configuration (California Irrigation Management Information System)
# Get your API key from: https://cimis.water.ca.gov/WSNReportCriteria.aspx
VITE_CMIS_API_KEY=your_cmis_api_key_here
VITE_CMIS_BASE_URL=https://api.cimis.water.ca.gov/api/data

# 📝 Note: The FROM_EMAIL can be committed as it's not sensitive
RESEND_API_KEY=your_resend_api_key_here
FROM_EMAIL=your_sender_email@yourdomain.com

# GitHub Actions Secrets (set these in GitHub repository secrets, NOT in .env)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here (GitHub secret only)

# Notes:
# - VITE_ prefixed variables are bundled into the frontend build
# - RESEND_API_KEY should be set in Supabase Edge Functions environment
# - Service role key should ONLY be in GitHub repository secrets