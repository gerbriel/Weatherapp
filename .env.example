# Environment Variables Template
# Copy this file to .env and fill in your actual values
# WARNING: Never commit .env files to git!

# Frontend Environment Variables (safe for client-side use)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Development/Testing Environment Variables
# These should NOT be set in production - use Supabase Edge Function environment variables instead
RESEND_API_KEY=your_resend_api_key_here
FROM_EMAIL=your_sender_email@yourdomain.com

# GitHub Actions Secrets (set these in GitHub repository secrets, NOT in .env)
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here (GitHub secret only)

# Notes:
# - VITE_ prefixed variables are bundled into the frontend build
# - RESEND_API_KEY should be set in Supabase Edge Functions environment
# - Service role key should ONLY be in GitHub repository secrets