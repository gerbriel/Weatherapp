name: Email Automation

on:
  schedule:
    # Run every 5 minutes (GitHub Actions minimum for free accounts)
    - cron: '*/5 * * * *'
  workflow_dispatch: # Allow manual triggering

jobs:
  send-weather-emails:
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger Weather Email Function
        run: |
          response=$(curl -s -X POST \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InVmbHZkY2NhbWhiZ2FxbmJ5Z2Z3Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2MTA4MTI2MywiZXhwIjoyMDc2NjU3MjYzfQ.Er53-sA447g3oOufA_UQbqIAkkC6Ydyg0L647zOfZ84" \
            "https://uflvdccamhbgaqnbygfw.supabase.co/functions/v1/send-weather-emails")
          echo "Response: $response"
        continue-on-error: true

      - name: Log Status
        run: |
          echo "Weather email automation check completed at $(date)"